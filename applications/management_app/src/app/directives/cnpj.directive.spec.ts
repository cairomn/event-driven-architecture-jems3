import { CNPJDirective } from './cnpj.directive';

describe('CNPJDirective', () => {
  it('should create an instance', () => {
    const directive = new CNPJDirective();
    expect(directive).toBeTruthy();
  });
});
